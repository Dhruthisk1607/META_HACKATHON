[project]
name = "csv-data-cleaner"
version = "0.1.0"
description = "OpenEnv CSV Cleaner Agent"

dependencies = [
    "fastapi",
    "uvicorn",
    "pandas",
    "pydantic",
    "requests"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
